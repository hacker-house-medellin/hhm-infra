groups:
  - name: hhm.availability
    rules:
      - alert: HHMServiceUnavailable
        expr: up{namespace=~"hhm.*"} == 0
        for: 5m
        labels: { severity: page }
        annotations:
          summary: "Hacker House Medellín service unavailable"
