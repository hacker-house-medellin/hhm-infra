Placeholder working directory for the shared `canonical` Supabase project. Rename this directory (and update
`.db-providers.json`) to the real 20-character project ref only after provider read-back. Schema namespace: `hacker_house_medellin`.
