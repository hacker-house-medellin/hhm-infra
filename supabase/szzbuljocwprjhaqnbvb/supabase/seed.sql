-- Preview/local seed for hacker-house-medellin (auth). Runs only on local stacks and Supabase preview branches.
-- Keep it idempotent, synthetic, and inside the hacker_house_medellin schema. Never add real user data or credentials.
create schema if not exists hacker_house_medellin;
